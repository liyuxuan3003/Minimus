# 文档编写规范

本文档定义了 LaTeX 模板工程各仓库用户文档的编写规范。

## 文件命名

每个 `.sty` 或 `.cls` 文件对应一个同名的 `.md` 文件，放置在同一目录下。

如 `minimus-math.sty` 对应 `minimus-math.md`。

## 文档结构

```markdown
# 包名

一句话说明包的用途。

## 引入的宏包

### `pkg1`、`pkg2`

简要说明这一组宏包的作用。

| 宏包 | 说明 |
|------|------|
| `pkg1` | 说明 |
| `pkg2` | 说明 |

## 自定义命令

### `\cmd1`、`\cmd2`

简要说明这组命令的作用。

| 命令 | 说明 |
|------|------|
| `\cmd1` | 说明 |

| 序号 | 格式 | 类型 | 说明 |
|------|------|------|------|
| 1 | `[arg]` | 可选 | 说明，默认值 |

## 自定义环境

### `EnvName`

简要说明该环境的作用。

```latex
\begin{Env}*[arg]{opt}!!
  ...
\end{Env}
```

| 序号 | 格式 | 类型 | 说明 |
|------|------|------|------|
| 1 | `*` | 标记 | 说明 |
| 2 | `[arg]` | 可选 | 说明，默认无 |
```

## 标题规范

### 二级标题

固定使用以下三个：

- `## 引入的宏包`
- `## 自定义命令`
- `## 自定义环境`

### 三级标题

- **仅包含名称**，用 `` ` `` 包裹，不附带描述和参数格式
- **相似命令可合并**到同一个标题，但每个命令名都必须出现在标题中
- 示例：`` ### `\N`、`\Z`、`\Q`、`\R`、`\C`、`\emptyset` ``
- 环境标题不含 `\begin`/`\end`，仅写环境名：`` ### `Equation` ``

## 内容规范

### 三级标题后必须跟一行简要说明

用一句话概括这部分命令或环境的用途，允许与下文表格内容有重复。

### 每个三级标题下必须有表格

- **宏包表格**（两列）：`宏包`、`说明`
- **命令表格**（两列）：`命令`、`说明`。有参命令在命令列带参数，如 `` `\Sum[sub][sup]` ``。说明列用内联 `$...$` 写数学输出。

### 有参命令/环境附加参数表格

四列：`序号`、`格式`、`类型`、`说明`

| 列 | 说明 |
|----|------|
| 序号 | 参数顺序 |
| 格式 | 实际书写形式，如 `[label]`、`{content}`、`*`、`!` |
| 类型 | 必选、可选、标记、分隔 |
| 说明 | 参数含义及默认值 |

### 环境以代码块签名开头

环境需要先用 ` ```latex ` 代码块给出完整签名，然后是参数表格，最后按需给出示例。

## 语言规范

- **中英文间不加空格**
- **代码内参数使用英文**：`\Int[low][high]` 而非 `\Int[下标][上标]`
- **代码块内全英文**：`` ```latex `` 或 `` ` `` 内的内容全部使用英文
- **禁止 Unicode 箭头符号** `→`，用文字替代（如"产生"、"输出"）

## 数学符号规范

内联 `$...$` 数学模式中**不得使用**如下包的命令（这些包在纯 Markdown 渲染环境中不可用）：

- `physics`：`\dd`、`\vb`、`\vb*` 等
- `upgreek`：`\uppsi`、`\updelta` 等
- `accents`：`\underaccent` 等

替代方案：使用标准 LaTeX 命令（`\mathrm`、`\mathbf`、`\mathbb`、`\operatorname` 等），或改用文字描述。
